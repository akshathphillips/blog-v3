{
  "name": "blog-v3",
  "version": "3.0.0",
  "private": true,
  "description": "Akshath Phillips' site. Zero-dependency static site generator.",
  "scripts": {
    "build": "node build.js",
    "test": "node --test test/*.test.js"
  }
}
